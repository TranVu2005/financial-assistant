"""Day 22 Dashboard submission bundle: raw question -> retrieval(live) ->
plan -> execution -> verification -> `SubmissionItem` + replayable CSV, then
a deterministic ZIP validated offline before upload.

See [docs/plans/day22-submission-export.md](../../../docs/plans/day22-submission-export.md).
"""
